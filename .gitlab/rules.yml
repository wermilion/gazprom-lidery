.from_dev:
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "web"

.from_prod:
  rules:
    - if: $CI_COMMIT_TAG

.from_test:
  rules:
    - if: $CI_OPEN_MERGE_REQUESTS